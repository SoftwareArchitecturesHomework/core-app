{
  "name": "core-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npx prisma migrate dev --name init --skip-seed && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@prisma/nuxt": "^0.3.0",
    "nuxt": "^4.2.1",
    "prisma": "^6.19.0",
    "vue": "^3.5.22",
    "vue-router": "^4.6.3"
  }
}
